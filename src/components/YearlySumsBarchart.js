import React, { PropTypes } from 'react';

const YearlySumsBarchart = (sums) => {
  return (<p>sums chart todo here</p>);
};

YearlySumsBarchart.propTypes = {
  sums: PropTypes.object.isRequired,
};

export default YearlySumsBarchart;
